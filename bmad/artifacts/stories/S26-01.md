# S26-01: Performance & Polish (3 pts)

**Sprint:** 26 (deferred to 27)
**Status:** Ready for development
**Points:** 3

## Description

Profile and optimize bundle size, validate large dataset performance (1000+ events), and polish UI edge cases introduced by ComfyUI integration. Post-release hardening focused on real-world usage patterns.

## Acceptance Criteria

1. **Bundle Analysis**
   - [ ] Generate bundle size report with `rollup-plugin-visualizer` or `webpack-bundle-analyzer` equivalent
   - [ ] Document gzip sizes for main.[hash].js, vendor chunks, CSS
   - [ ] Identify and remove dead code (Tailwind remnants, unused imports)
   - [ ] Report: `bmad/artifacts/bundle-analysis-2026-03-XX.md`

2. **Large Dataset Performance**
   - [ ] Playwright perf test: Add 100 events to timeline, measure scroll fps + paint time
   - [ ] Add 1000 events, verify no visible lag on timeline scroll
   - [ ] IndexedDB batch ops optimized (no N+1 queries)
   - [ ] Screenshot test: 1000-event timeline renders without layout shift

3. **ComfyUI Panel Polish**
   - [ ] Settings panel respects viewport height (use `max-height: calc(100dvh - 200px)`)
   - [ ] No overflow hidden on comfy settings — scrollable if content exceeds
   - [ ] Dark mode toggle persists to localStorage (key: `theme-preference`)
   - [ ] Theme applies without white flash on page load (inject into `<head>` before DOM parse)

4. **localStorage & Persistence**
   - [ ] Audit all localStorage usage: theme, user prefs, ComfyUI settings
   - [ ] Remove abandoned keys from previous sprints
   - [ ] Add `version` key to invalidate old data on schema changes
   - [ ] All 430 tests passing

## Technical Notes

- Review `src/routes/app/+layout.svelte` for theme injection timing
- Check `src/lib/stores/comfy-settings.ts` for IndexedDB perf (batch writes)
- CSS: `color-scheme` property already applied (no .dark class needed)
- Use `container-query` for ComfyUI panel if responsive tweaks needed

## Definition of Done

- [ ] Bundle report generated and documented
- [ ] Perf tests added: `src/routes/app/perf.spec.ts`
- [ ] All visual tests passing (1000-event screenshot)
- [ ] localStorage cleanup complete
- [ ] No regressions on existing tests
- [ ] PR merged to main
