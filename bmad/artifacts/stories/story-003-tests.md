# STORY-003: Ensure tests run locally

Title: Run and fix tests
Points: 3

Description:
Run `pnpm run test:unit` and `pnpm run test:e2e` locally; address any failing tests or missing mocks.

Acceptance:

- Unit tests pass locally in the dev environment.
- Any test failures are recorded as bugs and linked to dev stories.
