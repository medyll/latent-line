# ST-004 — SequenceOrchestrator Logic

**Sprint:** 1
**Status:** Done
**Priority:** Medium

---

## Description

Add interactivity to SequenceOrchestrator: event selection, `lang="ts"`, and removal of the stale `Object.values()` wrapper.

## Acceptance Criteria

- [x] `<script>` tag has `lang="ts"`
- [x] `Object.values()` wrapper removed — `exampleModel.timeline` is already an array
- [x] Stale defensive comment removed
- [x] Clicking a synoptic card or temporal card selects that event
- [x] Both views highlight the same selected event
- [x] `selectedTime` exposed as `$bindable` for parent integration

## Technical Notes

- Selection key is `event.time` (unique per event in the model)
- Both views iterate the same `timelineStore` array — sync is trivial via shared reactive state

## Implementation Notes

**Date:** 2026-03-05
**Files changed:**
- `src/lib/components/app/SequenceOrchestrator.svelte` — TypeScript, array fix, selection state

**Notable decisions:**
- `selectedTime: number | null` matches `event.time` as primary key (no id on raw TimelineEvent)
