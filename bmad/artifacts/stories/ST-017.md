# ST-017 — Event CRUD in SequenceOrchestrator

- sprint: 4
- created: 20260308T
- status: done
- completed: 2026-03-08T18:14:00Z
- points: 3

## Description

Add Add / Delete / Duplicate event actions to SequenceOrchestrator. Events are mutated directly on `model.timeline` via context.

## Implementation Notes

**Date:** 2026-03-08
**Files changed:**
- `src/lib/components/app/SequenceOrchestrator.svelte` — addEvent / deleteEvent / duplicateEvent mutating model.timeline via context; mood badge on thumbnails
- `src/lib/components/app/SequenceOrchestrator.test.ts` — 10 tests for CRUD logic

**Notable decisions:**
- Duplicate bumps time until a free slot is found to avoid collisions
- `confirm()` used for delete — no external dialog dependency needed

## Acceptance Criteria

- "Add event" button inserts a new TimelineEvent at the end with time = max(time)+10
- "Delete" button on each thumbnail removes the event (after confirm)
- "Duplicate" clones the event with a new time = original.time + 1
- Synoptic view shows mood indicator badge when an actor has speech.mood
- Unit tests for Add/Delete/Duplicate logic
