# ST-016 — Model context + SequenceOrchestrator integration

- sprint: 4
- created: 20260308T
- status: done
- completed: 2026-03-08T18:12:00Z
- points: 3

## Description

Add a shared `MODEL_STORE_KEY` context. Replace the hardcoded `exampleModel` in SequenceOrchestrator with the reactive model from context. Wire the center pane in `app/+page.svelte` to use SequenceOrchestrator with shared selection state (`selectedTime`) bridging to PropertiesPanel.

## Implementation Notes

**Date:** 2026-03-08
**Files changed:**

- `src/lib/context/keys.ts` — added MODEL_STORE_KEY symbol
- `src/lib/context/keys.test.ts` — added 3 tests for MODEL_STORE_KEY
- `src/lib/components/app/SequenceOrchestrator.svelte` — reads timeline from MODEL_STORE_KEY context
- `src/routes/app/+page.svelte` — single model $state, SequenceOrchestrator in center pane, selectedTime→selectedEventId bridge

**Notable decisions:**

- Full model as $state<Model> in app page; context exposes both ASSET_STORE_KEY and MODEL_STORE_KEY
- PropertiesPanel timelineEventsForPanel removed in favour of direct context mutation (ST-019)

## Acceptance Criteria

- `MODEL_STORE_KEY` exported from `src/lib/context/keys.ts`
- SequenceOrchestrator reads timeline from context (not exampleModel import)
- app/+page.svelte: center pane shows SequenceOrchestrator
- Clicking a timeline event in SequenceOrchestrator → PropertiesPanel shows that event's frame
- Unit tests pass
