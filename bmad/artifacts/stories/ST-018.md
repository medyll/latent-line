# ST-018 — SystemFooter: seed, import, Zod-validated export

- sprint: 4
- created: 20260308T
- status: done
- completed: 2026-03-08T18:16:00Z
- points: 3

## Description

Connect SystemFooter to `model.config` via context. Add seed numeric input with random button. Export button validates via `modelSchema.safeParse()` and shows inline errors. Add import button to load model.json.

## Implementation Notes

**Date:** 2026-03-08
**Files changed:**
- `src/lib/components/app/SystemFooter.svelte` — complete rewrite: model.config binding, seed+random, Zod-validated export, file import
- `src/lib/components/app/SystemFooter.test.ts` — 7 tests for validation and seed logic
- `src/routes/app/+page.svelte` — added SystemFooter below PaneGroup

**Notable decisions:**
- Import mutates model properties in-place (not replaces) to preserve existing context references
- Export errors shown inline; no external toast library needed

## Acceptance Criteria

- Checkpoint, sampler, TTS engine selects are wired to model.config
- Seed: number input + random button (random 0–999999)
- Export: validates model first; shows Zod errors inline if invalid, otherwise downloads model.json
- Import: file picker accepting .json; validates on load; replaces model state if valid; shows errors if invalid
- Unit tests for export validation and import parsing logic
