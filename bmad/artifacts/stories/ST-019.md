# ST-019 — PropertiesPanel: full event editing

- sprint: 4
- created: 20260308T
- status: done
- completed: 2026-03-08T18:18:00Z
- points: 5

## Description

Expand PropertiesPanel to fully edit a selected timeline event's frame. Mutations must propagate back to `model.timeline` via context. Replace JSON previews for FX/ControlNet with proper sliders/inputs.

## Implementation Notes

**Date:** 2026-03-08
**Files changed:**

- `src/lib/components/app/PropertiesPanel.svelte` — complete rewrite: direct model.timeline mutations via context; camera zoom/pan/tilt; lighting type+intensity; FX bloom/motion_blur sliders; ControlNet type+strength; per-actor speech editor (text/mood/lip_sync/vol/pitch)
- `src/lib/components/app/PropertiesPanel-st019.test.ts` — 15 tests for mutation helpers
- `src/routes/app/+page.svelte` — simplified (removed timelineEventsForPanel)

**Notable decisions:**

- PropertiesPanel now uses getContext(MODEL_STORE_KEY) directly; no more $bindable timelineEvents prop
- ensureCamera/Lighting/FX/ControlNet helpers initialise sub-objects on demand to avoid null mutations

## Acceptance Criteria

- Camera: zoom slider + pan X/Y inputs + tilt input
- Lighting: type select + intensity slider
- FX: bloom slider (0–1) + motion_blur slider (0–1) — replaces JSON view
- ControlNet: type text input + strength slider (0–1) — replaces JSON view
- Speech editor per actor: text textarea, mood select (Mood enum), lip_sync toggle, volume/pitch inputs
- All mutations propagate to model.timeline via MODEL_STORE_KEY context
- Unit tests covering mutation helpers
