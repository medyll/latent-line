# PR (draft) — S14-04: Playback progress bar

Summary

- Story: S14-04 — UX: playback progress bar — scrub position display + time label
- Change: Adds a playback progress bar component that shows current playhead position (e.g. `0:03 / 0:45`) and allows scrubbing. Time label updates during play and when scrubbing.

Acceptance criteria (from sprint):
- Playback bar shows current time (e.g. `0:03 / 0:45`) that updates during play and on scrub
- Scrubbing updates the playhead position and fires an event to update timeline selection
- Component is keyboard accessible (left/right to step, home/end to jump)
- Unit tests cover time formatting and scrub interactions

Files changed (assumed)
- src/lib/components/player/playback-bar.svelte
- src/lib/hooks/use-playback.ts
- src/lib/utils/time-format.ts
- tests/player/playback-bar.test.ts

Changelog entry

- S14-04: UX — Add playback progress bar with scrub and time label

Notes / Assumptions

> Assumed: Implementation exists in a local branch (e.g. `feat/S14-04`) or as a set of commits. If the code is not present, run the implementation steps in `src/lib/components/player/` and add tests in `tests/player/` before opening PR.

> Note: Could not open a remote PR from this environment (no network). To create the PR locally:

```powershell
git checkout -b feat/S14-04
git add <files>
git commit -m "S14-04: playback progress bar — scrub + time label"
git push origin feat/S14-04
# Then open a PR on GitHub/GitLab with the above branch and use this draft as the description.
```

Review guidance

- Verify tests pass locally: `pnpm run test:unit -- tests/player/playback-bar.test.ts`
- Confirm accessibility (keyboard navigation + aria labels)
- Check interactions with timeline and events selection

Generated by bmad: continue (local PR draft)
